#include <iostream>

using namespace std;


float srednia(int n, int* g) {
    float sr;
    int i;
    sr = 0;
    for (i = 0; i < n; i++) sr = sr + g[i];
    sr = sr / n;

    return sr;
}
float suma(int n, int* T) {
    float sum;
    int i;
    sum = 0;
    for (i = 0; i < n; i++) sum = sum + T[i];

    return sum;
}
void zadanie2a(int a, int* tab1, int* tab2, int* tab3) {
    int i,x;

    for (i = 0; i < a; i++) tab3[i] = tab1[i] + tab2[i];
   
}
void tablica2d(int w,int k) {
    int j, y,max=0,s;
    double** tab2 = new double* [w];
    for (int i = 0; i < w; ++i)
    {
        tab2[i] = new double[k];
        for (int j = 0; j < k; ++j)
            tab2[i][j] = (i + j);
    }
    for (int i = 0; i < w; ++i, cout << endl)
        for (int j = 0; j < k; ++j)
            cout << tab2[i][j] << '\t';
 /*   for (int i = 0; i < w; ++i, cout << endl) {
        for (int j = 0; j < k; ++j) {
            if(tab2)
            s = tab2[i][j] + tab2[i - 1][j] + tab2[i][j - 1] + tab2[i + 1][j] + tab2[i][j + 1];
            if (s > max)
                max = s; 
        }  
    } */
    cout << "===MAX===" << endl << max;
    delete[] tab2;

    tab2 = NULL;
}
int main()
{
    int i, n, a,w,k;
    int* T;
    int* tab1;
    int* tab2;
    int* tab3;
    
    float sum, sr;
    cin >> n;
    T = new int[n];
    for (i = 0; i < n; i++) T[i] = 2 + i + i;
   // for (i = 0; i < n; i++) cout << T[i] << endl;

    // średnia
    sr = srednia(n, T);
    cout << "srednia= " << sr << endl;
    // dodawanie
    sum=suma(n,T);
    cout << "suma= " << sum << endl;
    delete[] T;
    //zadanie 2=========================================
    cout << " zadanie 2" << endl;
    cin >> a;
    tab1 = new int[a];
    //tablica 1
    cout << "tab1= " << endl;
    for (i = 0; i < a; i++) tab1[i] = 1 + i + i;
    for (i = 0; i < a; i++) cout << tab1[i] << endl;
    tab2 = new int[a];
    //tablica 2
    cout << "tab2= " << endl;
    for (i = 0; i < a; i++) tab2[i] = 2 + i + i;
    for (i = 0; i < a; i++) cout << tab2[i] << endl;
    //tablica 3
    tab3 = new int[a];
    cout << "tab3= " << endl;
    zadanie2a(a, tab1, tab2, tab3);
    for (i = 0; i < a; i++) cout << tab3[i] << endl;
    delete[] tab1;
    delete[] tab2;
    delete[] tab3; 
    //zadanie 3a===============================
    cout << "Liczba wierszy: ";
    cin >> w;
    cout << "Liczba kolumn: ";
    cin >> k;
    tablica2d(w,k);

    

    system("PAUSE");
    return 0;

}

